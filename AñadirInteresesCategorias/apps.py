from django.apps import AppConfig

class AppGuardarInteresesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app_guardar_intereses'  # Asegúrate de que este nombre coincida con el nombre de tu aplicación
